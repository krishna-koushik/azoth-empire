scalar Date
scalar Email
scalar Phone
scalar JSON
scalar Password
scalar JWT

type PageInfo {
  hasPreviousPage: Boolean!
  hasNextPage: Boolean!
  startCursor: String
  endCursor: String
}

enum OrderDirection {
  ASC
  DESC
}
